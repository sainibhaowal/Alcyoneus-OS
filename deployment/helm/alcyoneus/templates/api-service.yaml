apiVersion: v1
kind: Service
metadata:
  name: {{ include "alcyoneus.fullname" . }}-api
  labels:
    {{- include "alcyoneus.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  type: {{ .Values.api.service.type }}
  ports:
    - port: {{ .Values.api.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.api.service.metricsPort }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "alcyoneus.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api